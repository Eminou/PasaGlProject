<h1>Manager creation</h1>

<form action="{{ path('manager_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('manager') }}">
            Back to the list
        </a>
    </li>
</ul>
