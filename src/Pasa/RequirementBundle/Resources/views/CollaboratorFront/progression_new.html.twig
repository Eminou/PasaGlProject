{% extends 'PasaRequirementBundle::collaborator_layout.html.twig' %}

{% block content %}
<h1>Progression creation</h1>

<form action="{{ path('collaboratorfront_progression_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('collaboratorfront_progression') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}